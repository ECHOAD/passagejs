{
  "name": "passage-zone",
  "version": "0.0.1",
  "publishConfig": {
    "access": "restricted"
  },
  "private": true,
  "scripts": {
    "build": "lerna run prepare --parallel",
    "buidl": "lerna run buidl --parallel",
    "bootstrap": "lerna bootstrap --use-workspaces",
    "lint": "lerna run lint",
    "format": "lerna run format",
    "test": "lerna run test --stream",
    "codegen": "lerna run codegen --stream"
  },
  "devDependencies": {
    "@babel/cli": "7.26.4",
    "@babel/core": "7.26.0",
    "@babel/eslint-parser": "^7.25.9",
    "@pyramation/babel-preset-env": "0.2.0",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "^29.7.0",
    "eslint": "9.17.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.1",
    "jest": "29.7.0",
    "lerna": "8.1.9",
    "prettier": "3.4.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/pyramation/badkidsjs"
  }
}
